<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Signup</title>
  <link rel="stylesheet" href="styles.css">
</head>
<body>
  <div class="container">
    <!-- Left Section -->
    <div class="left-section">
    <h2>THE WORLD IS CHANGING</h2>
    <h1>CONNECT WITH <br> PEOPLE AROUND <br> THE WORLD.</h1>
    <p>Already have an account?</p>
      <%= link_to "Log in →", new_user_session_path, class: "login-link" %>
    </div>

    <!-- Right Section -->
    <div class="right-section">
      <div class="login-box">
        <h3>Create your account</h3>
        
        <!-- Devise Sign Up Form -->
        <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
          <%= devise_error_messages! %>

          <div class="input-group">
            <%= f.label :first_name, "First Name" %>
            <%= f.text_field :first_name, id: "first_name", placeholder: "Your first name" %>
          </div>

          <div class="input-group">
            <%= f.label :last_name, "Last Name" %>
            <%= f.text_field :last_name, id: "last_name", placeholder: "Your last name" %>
          </div>

          <div class="input-group">
            <%= f.label :username, "Username" %>
            <%= f.text_field :username, id: "username", placeholder: "Your username" %>
          </div>

          <div class="input-group">
            <%= f.label :email, "Email" %>
            <%= f.email_field :email, id: "email", placeholder: "youremail@example.com" %>
          </div>

          <div class="input-group">
            <%= f.label :password, "Password" %>
            <%= f.password_field :password, id: "password", placeholder: "••••••••" %>
          </div>

          <div class="input-group">
            <%= f.label :password_confirmation, "Confirm Password" %>
            <%= f.password_field :password_confirmation, id: "confirm-password", placeholder: "••••••••" %>
          </div>

          <div class="actions">
            <%= f.submit "Sign Up", class: "form-login-button" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</body>
</html>